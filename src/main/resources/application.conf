port = 8080
connectionString = "jdbc:postgresql://localhost:5432/crud-poc?user=admin&password=admin"

jwt {
    issuer = "https://jwt-provider-domain/"
    audience = "jwt-audience"
    realm = "ktor sample app"
    subject = "Authentication"
    secret = "secret"
}

consume {
    googleApi {
        host = "maps.googleapis.com"
        key = "AIzaSyC0mBA56VsOHRF7RmMWBZb_cU9V7q8Jop0"
    }
}